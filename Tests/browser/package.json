{
  "name": "samedesk-browser-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright smoke tests for the SameDesk browser client (client.html / client.js).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "setup": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
